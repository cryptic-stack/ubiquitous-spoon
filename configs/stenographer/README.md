# Stenographer Config Placeholder

Stenographer configuration will live here.

MVP goal:

- Maintain a local rolling full packet capture buffer on sensor profiles.
- Keep full packet capture local to the sensor, following Security Onion design principles.
- Support retrieval by time window and filter once the API is implemented.

Current state:

- Compose uses a placeholder container so profiles and health checks can be built before final Stenographer image/config decisions.

