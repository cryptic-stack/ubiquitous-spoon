# Arkime Config Placeholder

Arkime capture and viewer configuration will live here.

MVP goal:

- Capture packet/session metadata on sensor profiles.
- Store local packet data or session data under `/data/arkime`.
- Expose Arkime Viewer where appropriate.

Current state:

- Compose uses a placeholder container so profiles and health checks can be built before final Arkime image/config decisions.

